# Normalize line endings to LF in the repo. Shell scripts must be LF to run
# inside Linux containers regardless of the host that committed them.
* text=auto eol=lf
*.sh text eol=lf
*.png binary
*.jpg binary
*.gif binary
