# EXAMPLE ONLY. Do not put real values here and do not apply this file.
#
# Generate the real Secret locally with:  ./k8s/generate-secret.sh
# That writes k8s/02-secret.yaml (git-ignored) with freshly generated values.
#
# The encryption key must be identical for the main process and every worker,
# which is automatic here because they all read this one Secret.
apiVersion: v1
kind: Secret
metadata:
  name: n8n-secret
  namespace: n8n
type: Opaque
stringData:
  N8N_ENCRYPTION_KEY: replace_with_openssl_rand_hex_32
  DB_POSTGRESDB_PASSWORD: replace_with_openssl_rand_hex_16
